'use client';

export default function Home() {
  return (
    <div className="flex">
      <div className="flex-grow flex items-center justify-center h-screen font-[family-name:var(--font-geist-sans)]">
        {/* Conteúdo da página inicial */}
        <h1 className="text-white text-2xl">Bem-vindo ao sistema ERP</h1>
      </div>
    </div>
  );
}